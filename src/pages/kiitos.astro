---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Kiitos viestistä | RAAKAA Podcast"
  description="Kiitos yhteydenotosta. Viestisi on vastaanotettu."
  noindex={true}
>
  <div class="page max">
    <section class="thanks glass">
      <p class="eyebrow">Onnistui</p>
      <h1>Kiitos <span>viestistäsi</span>!</h1>
      <p class="muted">Viestisi on vastaanotettu. Palaan asiaan mahdollisimman pian.</p>
      <div class="actions">
        <a class="btn" href="/">Takaisin etusivulle</a>
        <a class="btn alt" href="/jaksot">Selaa jaksoja</a>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .page {
    padding-top: 7rem;
  }

  .thanks {
    padding: clamp(1.4rem, 4vw, 2.6rem);
    max-width: 680px;
  }

  .eyebrow {
    color: var(--accent);
    text-transform: uppercase;
    letter-spacing: 0.25em;
    font-size: 0.7rem;
    font-weight: 700;
    margin: 0 0 0.8rem;
  }

  h1 {
    margin: 0 0 0.6rem;
    font-size: clamp(1.8rem, 5vw, 2.8rem);
    text-transform: uppercase;
    font-style: italic;
  }

  h1 span {
    color: var(--accent);
  }

  .actions {
    margin-top: 1.2rem;
    display: flex;
    gap: 0.7rem;
    flex-wrap: wrap;
  }
</style>
